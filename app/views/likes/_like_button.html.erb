<%# いいねされているかどうかにより表示を変更 %>
<% if current_user.likes.exists?(post: post) %>
  <%= button_to "🩷", post_like_path(post, current_user.likes.find_by(post: post).id), method: :delete, remote: true %> <!--非同期処理を有効に-->
<% else %>
  <%= button_to "🤍", post_likes_path(post), remote: true %>
<% end %>
