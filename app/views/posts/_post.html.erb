<div class="post-item">
  <p><%= post.contents %></p>
  <p>投稿者: <%= post.user.name %></p>

  <!-- いいねボタン -->
  <% if post.likes.exists?(user: current_user) %>
    <%= link_to post_like_path(post, current_user.likes.find_by(post: post)), method: :delete do %>
      💔
    <% end %>
  <% else %>
    <%= link_to post_likes_path(post), method: :post do %>
      🩷
    <% end %>
  <% end %>
</div>
